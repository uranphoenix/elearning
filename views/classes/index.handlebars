<h2>Classes</h2>
{{#each classes}}
    <div class="block">
        <h3>{{title}}</h3>
        <p>{{description}}</p>
        <p>Instructor: <strong>{{instructor}}</strong></p>
        <a class="button" href="/classes/{{_id}}/details">View Class</a>
    </div>
{{/each}}